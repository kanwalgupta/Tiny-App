<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
  <title>TinyApp</title>
</head>
<body>
    <ul>
    <% for (url in urls) { %>
        <li><%= url %> - <%= urls[url]%><a href = "#">Click for shortening this url</a><form method="POST" action="/urls/<%= url %>/delete"><input type="submit" value="Delete"></form></li>
    <% } %>
</ul>
</body>
</html>
