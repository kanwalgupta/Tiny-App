<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
  <title>TinyApp</title>
</head>
<body>
    <ul>
    <% for (url in urls) { %>
        <li><%= url %> - <%= urls[url]%><a href = "#">Click for shortening this url</a></li>
    <% } %>
</ul>
</body>
</html>
